# Wallapop-deal-finder
Wallapop deal finder currently featuring motorbikes. It scrapes data from wallapop, then runs a local (tiny) AI model to extract the relevant info (milage, condition, year, "red-flags",...) from the item's description. Finaly, it runs a regression model to detect good deals using the extracted info.
